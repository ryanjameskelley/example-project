export interface PrototypeConfig {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  temporary?: boolean;
}

const config: PrototypeConfig = {
  id: '1769063598584',
  title: '•  Create A Dialog',
  description: '•  create a dialog component using the shadcn design and the dialog in molecules in @/components/mol',
  createdAt: '2026-01-22',
  temporary: true,
};

export default config;