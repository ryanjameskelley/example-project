export interface PrototypeConfig {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  temporary?: boolean;
}

const config: PrototypeConfig = {
  id: '1768941372182',
  title: 'Adjust The Border Radius To',
  description: 'adjust the border radius to more rounded',
  createdAt: '2026-01-20',
  temporary: true,
};

export default config;