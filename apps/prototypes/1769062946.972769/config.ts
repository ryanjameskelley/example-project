export interface PrototypeConfig {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  temporary?: boolean;
}

const config: PrototypeConfig = {
  id: '1769062990542',
  title: 'Create A Dialog Component Using',
  description: 'create a dialog component using the shadcn design and the dialog in molecules',
  createdAt: '2026-01-22',
  temporary: true,
};

export default config;