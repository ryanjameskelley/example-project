export interface PrototypeConfig {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  temporary?: boolean;
}

const config: PrototypeConfig = {
  id: '1769050848785',
  title: '_using The DataTable Component With',
  description: '_using the DataTable component with @tanstack/react-table and the shadcn Table components_ create a ',
  createdAt: '2026-01-22',
  temporary: true,
};

export default config;